const rules = {
  name: [
    { required: true, message: 'nombre es requerido' }
  ],
  code: [
    { required: true, message: 'codigo es requerido' }
  ],
  faculty: [
    { required: true, message: 'facultad es requerido' }
  ]
}

export default rules
